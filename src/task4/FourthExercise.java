package task4;

public class FourthExercise {
    public static void main(String[] args) {

	//Integers only stores integral values, that is whole numbers (without a decimal point). 
	//Floating-point numbers are numbers that have fractional parts (usually expressed with a decimal point).
    //Floating-point numbers are used when more precision is needed.
  }
}

